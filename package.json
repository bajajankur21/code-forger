{
  "name": "code-forger-backend-tooling",
  "private": true,
  "version": "0.0.0",
  "description": "Dev tooling (commitlint + husky) for the Code Forger backend repo.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
